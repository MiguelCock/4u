[project]
name = "backend-data-collection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "python-dotenv>=1.2.2",
    "packages",
]

[tool.uv.sources]
packages = { path="../packages", editable=true }