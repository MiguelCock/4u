[project]
name = "packages"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MiguelCock", email = "maiguec999@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "supabase>=2.31.0",
]

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
