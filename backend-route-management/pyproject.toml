[project]
name = "backend-route-management"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "packages",
    "python-dotenv>=1.2.3",
]

[tool.uv.sources]
packages = { path = "../packages", editable = true }

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
